{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist", "bin", ".next", "storybook-static"]
    },
    "dev": {
      "dependsOn": ["^dev"],
      "outputs": ["dist", ".next"]
    },
    "clean": {}
  }
}
