{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist", "bin"]
    },
    "dev": {
      "dependsOn": ["^dev"],
      "outputs": ["dist"]
    },
    "clean": {}
  }
}
