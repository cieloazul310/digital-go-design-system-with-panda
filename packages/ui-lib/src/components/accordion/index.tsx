import { Accordion } from "@ark-ui/react/accordion";
import { styled } from "@import-map-package/styled-system/jsx";
import { accordion } from "@import-map-package/styled-system/recipes";

export const Root = styled(Accordion.Root, accordion);