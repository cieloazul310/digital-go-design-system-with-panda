import { ark } from "@ark-ui/react/factory";
import { legend } from "@digital-go-pandacss/styled-system/recipes";
import { styled } from "@digital-go-pandacss/styled-system/jsx";

export const Legend = styled(ark.legend, legend);
