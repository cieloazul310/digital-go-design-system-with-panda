import { ark } from "@ark-ui/react/factory";
import { requirementBadge } from "@import-map-package/styled-system/recipes";
import { styled } from "@import-map-package/styled-system/jsx";

export const RequirementBadge = styled(ark.span, requirementBadge);
