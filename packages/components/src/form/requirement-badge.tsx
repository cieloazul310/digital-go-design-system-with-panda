import { ark } from "@ark-ui/react/factory";
import { requirementBadge } from "@digital-go-pandacss/styled-system/recipes";
import { styled } from "@digital-go-pandacss/styled-system/jsx";

export const RequirementBadge = styled(ark.span, requirementBadge);
