import { ark } from "@ark-ui/react/factory";
import { requirementBadge } from "@cieloazul310/styled-system/recipes";
import { styled } from "@cieloazul310/styled-system/jsx";

export const RequirementBadge = styled(ark.span, requirementBadge);
