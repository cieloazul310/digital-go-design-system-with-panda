import { ark } from "@ark-ui/react/factory";
import { supportText } from "@import-map-package/styled-system/recipes";
import { styled } from "@import-map-package/styled-system/jsx";

export const SupportText = styled(ark.p, supportText);
