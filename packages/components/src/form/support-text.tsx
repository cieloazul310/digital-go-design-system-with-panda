import { ark } from "@ark-ui/react/factory";
import { supportText } from "@digital-go-pandacss/styled-system/recipes";
import { styled } from "@digital-go-pandacss/styled-system/jsx";

export const SupportText = styled(ark.p, supportText);
