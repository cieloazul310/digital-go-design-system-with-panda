import { ark } from "@ark-ui/react/factory";
import { label } from "@import-map-package/styled-system/recipes";
import { styled } from "@import-map-package/styled-system/jsx";

export const Label = styled(ark.label, label);
