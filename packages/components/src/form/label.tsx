import { ark } from "@ark-ui/react/factory";
import { label } from "@digital-go-pandacss/styled-system/recipes";
import { styled } from "@digital-go-pandacss/styled-system/jsx";

export const Label = styled(ark.label, label);
